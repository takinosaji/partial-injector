[project]
name = "sversion"
version = "2.5.0"
description = "Simple version management in Python"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Kostiantyn Chomakov", email = "kostiantyn.chomakov@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
  "toml>=0.10.2",
]

[project.urls]
Homepage = "https://github.com/takinosaji/partial-injector"
Repository = "https://github.com/takinosaji/partial-injector"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sversion"]

[tool.ruff]
line-length = 88
target-version = "py314"
extend-exclude = [".venv", "dist", "build", ".mypy_cache", ".pytest_cache", ".ruff_cache"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "SIM"]
ignore = ["E501", "UP042"]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]

[tool.ruff.lint.isort]
known-first-party = ["sversion"]
